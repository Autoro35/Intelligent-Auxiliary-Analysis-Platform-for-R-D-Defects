# 开发问题与解决记录

> 本文档按阶段记录项目开发过程中遇到的实际问题、排查过程与解决办法。
>
> 问题按性质分为四类，便于区分「产品缺陷」与「环境/工具干扰」：
> - 🐞 **代码缺陷** —— 代码本身的错误，会影响功能正确性
> - 🔧 **环境问题** —— 开发机/工具链导致的障碍，与代码质量无关
> - 🧪 **验证方法问题** —— 测试脚本自身的错误，曾一度误导排查方向
> - ⚖️ **设计权衡** —— 有意识做出的取舍，非缺陷

**项目**：轻量化研发缺陷全生命周期管理与智能辅助分析平台
**周期**：7 个阶段，2026-09-08 ~ 2026-09-12

---

## 阶段 1：项目骨架与数据层

### 1.1 🔧 本机无 JDK 17，只有 JDK 24

**现象**：项目规范要求 Java 17，开发机仅安装 JDK 24。

**解决**：`maven-compiler-plugin` 配置 `<release>17</release>`，用 JDK 24 编译出兼容 Java 17 的字节码。代码中不使用任何 17 以上版本的语法特性。

### 1.2 🐞 Lombok 在 JDK 24 下静默失效

**现象**：编译报大量「找不到符号」，`@Getter`、`@Slf4j`、`@AllArgsConstructor` 全部未生成代码。**没有任何报错指向 Lombok**，排查困难。

**根因**：Spring Boot 3.2.5 默认管理的 Lombok 版本为 1.18.30，该版本不支持 JDK 24。且 Maven 编译器插件在 JDK 24 下无法自动发现注解处理器，Lombok 被完全跳过。

**解决**（两处缺一不可）：
1. `pom.xml` 显式固定 `<lombok.version>1.18.38</lombok.version>`
2. `maven-compiler-plugin` 显式声明 `<annotationProcessorPaths>` 指向 Lombok

**经验**：Lombok 静默失效时不会有任何提示，遇到「符号找不到但代码看着没问题」优先怀疑注解处理器未生效。

---

## 阶段 2：用户认证与权限

### 2.1 🔧 Git Bash 内联中文 JSON 被错误编码（贯穿全程）

**现象**：`curl -d '{"title":"中文"}'` 发送的请求体在服务端解析失败，抛 `HttpMessageNotReadableException`，返回「请求体格式错误」。同样的 JSON 用 Postman 发送则正常。

**根因**：Git Bash（Windows）在传递命令行参数时按 GBK 编码，中文字节被破坏。

**解决**：所有含中文的请求体写入 UTF-8 的 `.json` 文件，用 `curl --data-binary @file` 发送。后续所有阶段的测试脚本都遵循此约定。

### 2.2 🐞 两层鉴权职责划分

**问题**：认证（你是谁）与授权（你能做什么）混在一起会导致逻辑纠缠。

**解决**：拆成两层——
- `JwtInterceptor`：解析令牌、写入 `UserContext`，只负责认证
- `@RequireRole` 注解 + `PermissionAspect`：只负责授权

数据层再按项目成员做第三次校验（多项目隔离）。

---

## 阶段 3：项目管理与缺陷工作流

### 3.1 🧪 清理数据后自增 ID 未重置，导致断言失败

**现象**：冒烟测试中创建缺陷后按 `id=1` 发起状态流转，连续三次返回「缺陷不存在(3001)」。

**根因**：清理数据库用的是 `DELETE FROM`，**不会重置自增计数器**，新记录的 ID 实际是 3。测试脚本硬编码了错误的 ID。

**解决**：从创建接口的响应中动态取 ID，不再假设 ID 从 1 开始。后续脚本统一采用 `field()` 辅助函数提取。

### 3.2 🐞 `assertMember` 管理员绕过存在性校验，产生孤儿数据

**现象**：管理员给一个**根本不存在的 projectId** 创建缺陷，接口返回「创建成功」。数据库中该缺陷的 `project_id` 指向不存在的项目。

**根因**：`assertMember` 的 `isAdmin()` 判断早于项目存在性校验，管理员直接 `return`，跳过了整个校验：

```java
// 问题代码
public void assertMember(Long projectId) {
    if (isAdmin()) {
        return;              // ← 管理员在此返回，项目存在性从未校验
    }
    Project project = getById(projectId);
    if (project == null) { throw ... }
}
```

**解决**：把存在性校验提到 `isAdmin()` 之前——**管理员豁免的只是「权限判断」，不豁免「数据存在性」**。

```java
Project project = getById(projectId);
if (project == null) { throw new BusinessException(ResultCode.PROJECT_NOT_FOUND); }
if (isAdmin()) { return; }
```

**发现时机**：阶段 5 冒烟测试时误建出一条孤儿缺陷才暴露。**修复见提交 `f99a1b6`。**

---

## 阶段 4：辅助功能与统计

### 4.1 🐞 Java 17 不支持 switch 模式匹配

**现象**：统计服务中用 `switch (e) { case DefectStatusEnum s -> s.getCode(); ... }` 编译失败。

**根因**：switch 模式匹配（Pattern Matching for switch）在 Java 17 中是**预览特性**，`--release 17` 下不可用。

**解决**：改用 `if (e instanceof XxxEnum)` 链。这一约束在后续阶段写代码时需持续注意。

### 4.2 🔧 临时目录被清空，工具链需重新准备

**现象**：新阶段开始时，之前下载的 Maven 报「文件不存在」。

**根因**：开发机无全局 Maven，验证时下载到 `/tmp`；系统重启后临时目录被清空。

**解决**：重新下载。**同类问题在阶段 6 也出现过**（Node.js 工具链），属该开发环境的固有特点。

### 4.3 🐞 逻辑删除导致 `COUNT(*)` 与实际数据量不符

**现象**：测试后清理数据库，`SELECT COUNT(*)` 仍显示有数据。

**根因**：项目全局启用 MyBatis-Plus 逻辑删除，删除是 `UPDATE ... SET deleted=1`，物理行仍在。

**解决**：清理测试数据时用**物理删除**（`DELETE FROM`），并在文档中提醒手写 SQL 查询时要带 `WHERE deleted = 0`。

---

## 阶段 5：AI 模块

### 5.1 ⚖️ 双层降级设计（关键设计决策）

**问题**：项目定位是「开箱即用」，但 DeepSeek 需要 API Key、Chroma 需要额外部署。若强依赖这两个外部服务，未配置时整个 AI 模块不可用。

**解决**：设计两层降级，保证**没有任何外部依赖时全流程仍可跑通**。

**大模型层**：

| 情况 | 行为 | 响应标识 |
|---|---|---|
| 已配置且调用成功 | 使用 DeepSeek | `source: "LLM"` |
| 未配置 Key | 本地关键词规则引擎 | `source: "RULE"` |
| 调用失败 | 回退规则引擎并标注 | `"RULE"` + 「AI 调用失败已降级」 |

**检索层**：向量检索 → MySQL ngram 全文索引 → 关键词模糊匹配，逐级降级，响应中 `retrievalMode` 标明实际生效的路径。

**收益**：这一设计在开发全程发挥了实际作用——由于本机一直没有 Chroma，RAG 功能始终跑在全文索引降级路径上，功能验证不受阻塞。

### 5.2 🐞 AI 分类提示词被界面词带偏（6 个用例错 2 个）

**现象**：真实接入 DeepSeek 后测试 6 条用例，2 条判错：

| 用例 | 错误结果 | 应有结果 | 原因 |
|---|---|---|---|
| 「订单提交后页面卡住，控制台报 500」 | `COMPATIBILITY` | `FUNCTIONAL` | 文中出现「**浏览器**控制台」 |
| 「删除按钮偶发无反应」 | `UI` / `CRITICAL` | `FUNCTIONAL` / `MAJOR` | 文中出现「**按钮**」；且「偶现」被判成致命 |

**根因**：提示词只给了枚举值定义，没给**消歧规则**。模型看到界面相关的词就往 UI/兼容性上靠。

**解决**：重写 `CLASSIFY_SYSTEM_PROMPT`，加入三部分：
1. **类型判定优先级**（按顺序匹配）：OPTIMIZATION → COMPATIBILITY → PERFORMANCE → UI → FUNCTIONAL
2. **反例约束**：明确指出「出现『浏览器控制台』不等于兼容性问题」「出现『按钮』不等于 UI 问题，只要点击后功能异常就判 FUNCTIONAL」
3. **频率约束**：标注「偶现/偶尔/个别」的，严重程度不得高于 MAJOR

**效果**：6 条用例全部判对。**修复见提交 `93b948d`。**

**经验**：给大模型的分类提示词，**反例比正例更重要**——仅给枚举定义时，模型会被表面词汇误导。

### 5.3 🐞 `assertOwner` 存在性校验顺序不一致（潜在陷阱）

**背景**：修复 `assertMember`（见 3.2）时，注意到 `assertOwner` 有相同的代码模式，当时一并记录为待办但未处理。

**核实后的真实情况**：`assertOwner` 的 4 个调用点（`update`/`delete`/`addMember`/`removeMember`）**各自都已做存在性校验**，因此**这不是活跃缺陷**，改动前后行为零差异。

**但仍做了修复**，理由是语义不一致会坑到未来的调用者：两个同为权限校验的方法，一个自校验、一个依赖调用方。

**解决**：
1. 调整顺序使其与 `assertMember` 一致
2. **在接口上写明契约**（比改代码更重要）：注明两个方法的完整语义，以及「调用方无需再自行做存在性校验」

**修复见提交 `3fe6c79`。**

**经验**：报告代码问题时**必须核实调用点**。仅凭代码模式判断「存在同样问题」可能把加固项误报成缺陷。

---

## 阶段 6：前端开发与联调

### 6.1 🔧 误判 Node.js 未安装（排查方法有误）

**现象**：`node -v` / `npm -v` 在 Git Bash 中报「命令未找到」，`where node` 也查不到。据此判断「本机未安装 Node.js」并下载了便携版。

**实际**：Node.js **已安装**，在 **`D:\nodejs`**（v22.16.0 + npm 10.9.2），是正规安装。

**两处排查疏漏**：
1. 只检查了 C 盘和几个常见安装位置，**没有检查 D 盘**；注册表 `HKLM/HKCU\SOFTWARE\Node.js` 也查不到，进一步误导了判断
2. **没有意识到 PATH 环境变量本身有问题**

**真正的根因**：持久化的用户 PATH 中，该条目被写成了 **`D:\nodejs"`** —— 末尾多了一个双引号。

```
Test-Path D:\nodejs"  →  报错：路径中具有非法字符
Test-Path D:\nodejs   →  True
```

`"` 是非法路径字符，CMD 严格匹配直接找不到；PowerShell 的路径解析较宽容，所以 `node -v` 在 PowerShell 里反而能输出 `v22.16.0`。**这个「CMD 报错、PowerShell 正常」的差异是定位问题的关键线索。**

**解决**：将 PATH 条目改为 `D:\nodejs`（用户的系统环境变量，已告知修法，由用户自行决定是否修改）。

**经验**：排查「命令未找到」时，**先确认 PATH 内容本身是否正确**，比反复搜索安装位置更有效。也应注意盘符可能不是 C。

### 6.2 🧪 测试脚本 `sed` 贪婪匹配，把业务字段当成响应状态码

**现象**：45 项接口契约测试中，7 项被误报为失败，但查看响应体明明是 `"code":200`。

**根因**：提取状态码用了 `sed -n 's/.*"code":\([0-9]*\).*/\1/p'`。`.*` 是**贪婪匹配**，会匹配到响应体中**最后一个** `"code":`。而项目 VO 里也有 `code` 字段（如项目编码 `"code":"E2E-2026"`），于是状态码被匹配成了空字符串。

**解决**：改用取**第一个**匹配的方式：

```bash
printf '%s' "$body" | grep -o '"code":[0-9]*' | head -1 | cut -d: -f2
```

**教训**：断言辅助函数本身出错时，会产生大量**误导性的失败**，浪费排查时间在正确无误的业务代码上。

### 6.3 🧪 `UID` 是 bash 只读变量

**现象**：脚本中 `UID=$(...)` 赋值报 `readonly variable`，导致变量为空，后续请求的 `userId` 为空，接口返回「用户不存在」。

**解决**：改用 `MEMBER_ID` 等不与 shell 内置变量冲突的名称。

### 6.4 🧪 `curl -F` 使用 `/tmp` 路径在 Windows 下无效

**现象**：附件上传测试返回空响应体。

**根因**：Git Bash 中的 `/tmp/...` 是 POSIX 路径，但 `curl` 是 Windows 原生程序，无法识别该路径。

**解决**：改用相对路径或 Windows 风格路径。

### 6.5 🐞 AI 补全描述会覆盖用户已填内容

**现象**：调用「AI 补全描述」后，用户原本填写的缺陷描述被 AI 生成的整合文本整个替换。

**解决**：区分两类返回内容——
- **分字段建议**（复现步骤/预期/实际/环境）→ 直接回填，因为这些字段服务端只产出对应内容
- **整合后的完整描述** → 放在右侧预览区，**由用户点击「采用」才写入**

**经验**：AI 辅助输入场景，默认行为应是「建议」而非「替换」，把最终决定权留给用户。

---

## 阶段 7：优化、容器化与文档

### 7.1 🐞 缓存键未带用户维度会导致跨用户数据串号（设计阶段拦截）

**问题**：为统计接口加缓存时发现，统计结果受**多项目隔离**影响——不同用户可见的项目集合不同。

**若用全局键** `stats:overview`，管理员看到的全量数据会被返回给只有 1 个项目的普通用户。

**解决**：缓存键带上用户维度 `defect:stats:overview:{userId}`，并在**缺陷/项目/知识库的写操作中主动失效**该前缀，保证写入后立刻读到新数据。

**说明**：此问题在编码前通过评审发现并规避，未流入运行环境。

### 7.2 🔧 注册接口限流导致测试脚本失败（并暴露一个功能缺口）

**现象**：回归测试脚本突然大面积失败，报错五花八门（400 请求体格式错误、404、401）。

**排查过程**：查后端日志发现根因是**注册接口返回 429**（5 次/5 分钟/IP 限流），导致脚本中 `$USER_ID` 为空，**空 ID 一路级联**出各种看似无关的错误。

**顺带发现的真实问题**：为了绕开限流，我改用「管理员建号」——**结果发现根本没有这个接口**。

**这是我本阶段自己造成的功能缺口**：本阶段移除了登录页的注册入口（因为注册即访客且无法自助升级），并在文档里写了「请用管理员在用户管理中创建」——**但用户管理页只有编辑/重置密码/删除，没有「新建用户」**。入口移除后，管理员实际上无法通过界面添加任何账号。写文档时想当然地以为有对应功能，没有回头核对。

**解决**（两件事）：
1. 新增 `POST /api/users`，管理员可直接建号并指定角色（复用注册逻辑，不受注册接口限流约束），前端补「新建用户」入口
2. 重写测试脚本：改用管理员建号接口，且**每一步都断言是否取到 ID，取不到立即中断**，避免空值级联出误导性错误

**修复见提交 `3fe6c79`。**

**经验**：移除一个入口前，要确认替代路径真的存在。**测试脚本应在早期步骤失败时立即中断**，而不是带着空值继续跑。

### 7.3 🐞 修复 N+1 查询时的自我纠错

**问题**：`DefectServiceImpl` 和 `KnowledgeServiceImpl` 在转 VO 时逐行查用户表，一页 10 条最多产生 20 次查询。

**第一次写错了**：

```java
// 错误：lambda 内部每次调用都会重新算一遍 map，等于没优化
return PageResult.of(defectPage, d -> toVO(d, loadUserMap(defectPage.getRecords())));
```

**解决**：把 map 的加载提到 lambda **外面**，只算一次。

```java
Map<Long, User> userMap = loadUserMap(defectPage.getRecords());
return PageResult.of(defectPage, d -> toVO(d, userMap));
```

**实测效果**：一次缺陷列表请求（3 条记录）总 SQL 4 条，其中查 `t_user` **仅 1 条**。

### 7.4 🔧 Docker 配置无法验证

**问题**：技术栈要求容器化，但开发机**未安装 Docker**。

**已做**：YAML 语法解析校验、引用路径存在性校验。
**未做**：`docker compose up` 从未执行，两个 Dockerfile 从未构建，nginx.conf 从未加载。

**处理**：如实记录在 [ToDoList.md](ToDoList.md) 第二节，列出全部风险点与排查方向，交由用户首次运行时验证。

---

## 附：经验总结

| 类别 | 教训 |
|---|---|
| **环境排查** | 报「命令未找到」时，先查 PATH 内容本身，别急着搜索安装目录；注意非 C 盘的安装位置 |
| **工具链** | Lombok 注解处理器失效是**静默**的；Windows 下 Git Bash 传中文参数会被 GBK 编码破坏 |
| **代码评审** | 报告问题前先核实调用点——「代码模式相同」不等于「缺陷相同」 |
| **权限校验** | 管理员豁免的应是**权限判断**，不是**数据存在性校验** |
| **AI 提示词** | 分类任务中，**反例约束比枚举定义更重要** |
| **AI 交互** | AI 辅助输入默认应是「建议」而非「替换」，最终决定权留给用户 |
| **缓存** | 涉及数据隔离的缓存，键必须包含隔离维度 |
| **测试脚本** | 辅助函数本身出错会产生大量误导性失败；应尽早断言、失败即停 |
| **进度管理** | 移除一个功能入口前，先确认替代路径已经存在 |
